import { PopupHostDirective } from './host.directive';

describe('HostDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupHostDirective();
    expect(directive).toBeTruthy();
  });
});
